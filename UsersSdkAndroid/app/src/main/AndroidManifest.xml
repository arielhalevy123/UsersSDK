<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.userssdkandroid">

    <!-- אינטרנט -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UsersSdkAndroid"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- זהו הלאונצ'ר היחיד של האפליקציה -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- מסך התחברות/הרשמה (נפתח פנימית ע"י MainActivity) -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.RegisterActivity"
            android:exported="false" />

        <!-- מסכי אדמין (נפתחים פנימית בלבד) -->
        <activity
            android:name=".ui.AdminAppointmentsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.AdminCalendarActivity"
            android:exported="false" />

        <!-- מסך יוזר (מארח את פרגמנט לוח השנה של הספרייה + FAB) -->
        <activity
            android:name=".ui.UserAppointmentsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.ProfileActivity"
            android:exported="false" />

        <!-- אם היה לך ProfileActivity ישן – לא נדרש יותר כשעובדים עם פרגמנטים.
             השארתי בחוץ בכוונה כדי למנוע כפל. -->
        <!-- <activity android:name=".ui.ProfileActivity" android:exported="false" /> -->

    </application>
</manifest>